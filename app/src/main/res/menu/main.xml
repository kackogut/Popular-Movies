<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_ordering"
        android:title="@string/main_menu_name"
        android:orderInCategory="1"
        app:showAsAction="ifRoom">
        <menu>
            <group android:checkableBehavior="single">
                <item android:id="@+id/order_popular"
                    android:title="@string/order_by_popular" android:checked="true" />
                <item android:id="@+id/order_top_rated"
                    android:title="@string/order_by_rating" />
            </group>
        </menu>
    </item>
</menu>